{
  "_meta": {
    "contract": "filigree-weft",
    "stability": "Frozen for the weft-generation lifetime per ADR-002.",
    "authority": [
      "docs/architecture/decisions/ADR-002-api-generations-and-federation-posture.md"
    ],
    "pinning_discipline": "shape reference; see docs/federation/contracts.md",
    "updated": "2026-04-26",
    "endpoint": "GET /api/weft/issues/{issue_id}/comments",
    "status": "live as of Phase C4.",
    "relation_to_classic": "Classic dashboard exposes only POST /api/issue/{id}/comments (no GET). Weft adds the GET counterpart and uses CommentRecordWeft (comment_id replacing classic id). Validates parent issue exists (404 NOT_FOUND if not) so missing issues do not silently return an empty list."
  },
  "shape_decl": {
    "request_type": "GET (no body, no query params)",
    "response_type": "ListResponse[CommentRecordWeft]",
    "rationale": "CommentRecordWeft = {comment_id, author, text, created_at}. Issue parent is validated upfront."
  },
  "examples": [
    {
      "name": "issue_not_found",
      "note": "Missing issue id → 404 NOT_FOUND. db.get_comments returns [] for unknown ids; the handler validates upfront so consumers see a real 404 instead of a misleading empty list.",
      "request": {
        "method": "GET",
        "path": "/api/weft/issues/genp-deadbeef00/comments",
        "headers": {},
        "body": null
      },
      "response": {
        "status": 404,
        "headers": {"Content-Type": "application/json"},
        "body": {"error": "Issue not found: genp-deadbeef00", "code": "NOT_FOUND"}
      }
    }
  ]
}
