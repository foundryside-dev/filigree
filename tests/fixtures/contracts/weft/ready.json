{
  "_meta": {
    "contract": "filigree-weft",
    "stability": "Frozen for the weft-generation lifetime per ADR-002.",
    "authority": [
      "docs/architecture/decisions/ADR-002-api-generations-and-federation-posture.md"
    ],
    "pinning_discipline": "shape reference; see docs/federation/contracts.md",
    "updated": "2026-04-26",
    "endpoint": "GET /api/weft/ready",
    "status": "live as of Phase C4.",
    "relation_to_classic": "Classic GET /api/ready returns a bare list of IssueDicts. Weft wraps in ListResponse[IssueWeft] with has_more always false (the underlying get_ready() is unbounded today)."
  },
  "shape_decl": {
    "request_type": "GET (no body, no query params)",
    "response_type": "ListResponse[IssueWeft]",
    "rationale": "get_ready() returns the full ready set in memory, so has_more is always false and next_offset is always omitted."
  },
  "examples": [
    {
      "name": "empty_or_seeded",
      "note": "Empty project (or one with auto-seeded planning issues) returns the unified envelope. Items shape is asserted by seeded shape-pin tests, not the fixture.",
      "request": {
        "method": "GET",
        "path": "/api/weft/ready",
        "headers": {},
        "body": null
      },
      "response": {
        "status": 200,
        "headers": {"Content-Type": "application/json"},
        "body": {"items": [], "has_more": false}
      }
    }
  ]
}
